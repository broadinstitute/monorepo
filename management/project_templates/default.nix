{ }:
{
  torch_gpu = {
    path = ./torch_pure;
    description = "Pytorch environment with GPU support.";
  };
  txtai_gpu = {
    path = ./txtai_gpu;
    description = "Txtai (RAG) environment with GPU support.";
  };
}
